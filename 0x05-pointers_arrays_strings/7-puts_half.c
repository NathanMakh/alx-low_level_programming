#include "main.h"

/**
 * void puts_half - returns half the length of a string
 * @s: string
 * Return: length
 */

void puts_half(char *str)
{
	char *str;
	int len;

	len = strlen(str);

	for (i = len / 2; i <= len / 2; i++)
		_putchar('i');
	_putchar('\n');
		if (i <= (n % i))
			_putchar('n');
}
