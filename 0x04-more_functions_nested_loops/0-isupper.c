#include "main.h"

/**
 * main - checks for uppercase letter
 * @c: character to be checked
 * Return: 1 if uppercase 0 if otherwise
 */

int _isupper(int c);
{
	int c;

	uppercase = 'A';
	lowercase = 'a';

	if (c == 'A')
	{
		putchar('%d', c);
		return (1);
	}
	else
	{
		putchar('%d', 'a');
		return (0);
	}
}
