#include "main.h"

/**
 * main - checks for uppercase letter
 * @c: character to be checked
 * Return: 1 if uppercase 0 if otherwise
 */

int _isupper(int c);
{
	int c;

	uppercase = 'A';
	lowercase = 'a';

	if (c >= 'A' && c <= 'a')
		return (1);
	else
		return (0)
