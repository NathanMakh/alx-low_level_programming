#include "main.h"

/**
 * int _isupper - checks for uppercase letter
 * @c: integer to be checked
 * Return: 1 if uppercase 0 if lowercase
 */

int _isupper(int c);
{
	int c;

	uppercase = 'A';

	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
