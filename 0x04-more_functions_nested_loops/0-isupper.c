#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase letter
 * @c: integer to be checked
 * Return: 1 if uppercase 0 if lowercase
 */

int _isupper(int c);
{
	int c;

	uppercase = 'A';
	lowercase = 'a';

	if (c == 'A')
	{
		putchar('%d', c);
		return (1);
	}
	else
	{
		putchar('%d', 'a');
		return (0);
	}
}
