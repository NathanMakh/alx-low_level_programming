#include "main.h"

/**
 * main - multiplies two integers
 * @a: first integer to be multiplied
 * @b: second integer to be multiplied
 * Return: mul
 */

int mul(int a, int b);
{
	mul = (ab);
	putchar('%d', mul);
	return (mul);
}
