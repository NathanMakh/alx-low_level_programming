#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * result: product of a and b
 * Return: result
 */

int mul(int a, int b)
{
	result = (a * b);
	return (result);
}
