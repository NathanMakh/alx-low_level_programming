#include "main.h"

/**
 * int mul - multiplies two integers
 * @a: first integer to be multiplied
 * @b: second integer to be multiplied
 * Return: always mul
 */

int mul(int a, int b);
{
	mul = (a, b);
	return (mul);
}
